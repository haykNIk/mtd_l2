# Контекст сборки: что НЕ отправлять в демон.
# В репозитории есть .git и build/ — им в образе делать нечего.
.git
.github
build
cmake-build-*

# Unit-тесты и скачанный в CI заголовок doctest в образе не нужны
src/tests

# Артефакты и файлы, не нужные приложению
*.png
*.md
